#ifndef CORE___H
#define CORE___H

#include <iostream>
#include <vector>
#include <map>
#include <string>

#include <SDL.h>
#include <SDL_image.h>

void warning(std::string reason, std::string warn);
void error(std::string reason, std::string err);

#endif